# 🇨🇳 国内访问部署指南

由于 Vercel 和 GitHub Pages 在中国大陆访问可能不稳定，本文档介绍如何部署到国内可访问的平台。

## 🎯 推荐方案

### 方案 1：Gitee Pages（已下线 ⚠️）

⚠️ **重要提示**：Gitee Pages 功能已于 2024 年 5 月 1 日下线，不再提供服务。

**替代方案：使用其他国内平台**

#### 步骤：

1. **注册 Gitee 账号**
   - 访问 https://gitee.com
   - 注册并登录

2. **导入 GitHub 仓库**
   - 在 Gitee 点击"+" → "导入仓库"
   - 输入 GitHub 仓库地址：`https://github.com/q755015108/resume_editor`
   - 点击"导入"

3. **配置 Gitee Pages**
   
   **重要：Gitee Pages 不在"服务"标签里！**
   
   **方法一：通过"管理"标签（推荐）**
   
   a. 在仓库页面顶部，点击 **"管理"** 标签
   
   b. 在左侧菜单中找到 **"Pages"** 或 **"Gitee Pages"** 选项
   
   c. 点击进入 Pages 设置页面
   
   d. 配置设置：
      - **分支**：选择 `main`
      - **目录**：选择 `/`（根目录）
      - 点击 **"启动"** 按钮
   
   **方法二：直接访问 Pages 设置页面**
   
   直接访问这个 URL（替换成你的用户名）：
   ```
   https://gitee.com/q755015108/resume_editor/pages
   ```
   
   或者：
   ```
   https://gitee.com/q755015108/resume_editor/settings/pages
   ```
   
   **方法三：通过仓库设置**
   
   a. 点击 **"管理"** 标签
   b. 在左侧菜单中找到 **"仓库设置"** 或 **"设置"**
   c. 在设置页面中找到 **"Pages"** 相关选项
   
   > ⚠️ **注意**：
   > - 如果仓库是私有的，可能需要升级到付费版才能使用 Pages
   > - 建议将仓库设置为**公开仓库**（公开仓库免费使用 Pages）
   > - 设置方法：管理 → 仓库设置 → 将仓库改为"公开"

4. **更新 vite.config.ts**
   ```typescript
   export default defineConfig({
     base: '/resume_editor/', // 你的仓库名
     // ... 其他配置
   })
   ```

5. **构建并部署**
   ```bash
   npm run build
   # 然后将 dist 目录推送到 Gitee
   ```

### 方案 2：Vercel（使用国内 CDN 加速）

如果继续使用 Vercel，可以配置国内 CDN 加速：

1. **使用 Cloudflare（免费）**
   - 注册 Cloudflare 账号
   - 添加你的域名
   - 配置 DNS，将域名指向 Vercel
   - Cloudflare 会自动提供 CDN 加速

2. **使用自定义域名**
   - 在 Vercel 项目设置中添加自定义域名
   - 使用国内域名服务商（如阿里云、腾讯云）的域名
   - 配置 DNS 解析

### 方案 3：Netlify（备选）

Netlify 在国内访问相对稳定一些：

1. 访问 https://www.netlify.com/
2. 导入 GitHub 仓库
3. 构建设置：
   - Build command: `npm run build`
   - Publish directory: `dist`

### 方案 4：国内云服务（最稳定）

#### 阿里云 OSS + CDN

1. **创建 OSS 存储桶**
   - 登录阿里云控制台
   - 创建 OSS 存储桶
   - 设置静态网站托管

2. **上传文件**
   ```bash
   npm run build
   # 使用阿里云 OSS 工具上传 dist 目录
   ```

3. **配置 CDN**
   - 绑定 CDN 域名
   - 配置 HTTPS
   - 国内访问速度会很快

#### 腾讯云 COS + CDN

类似阿里云，使用腾讯云的对象存储和 CDN 服务。

#### 七牛云（强烈推荐，有免费额度）

七牛云提供免费的对象存储和 CDN 服务，国内访问速度快。

**详细步骤：**

1. **注册七牛云账号**
   - 访问 https://www.qiniu.com
   - 注册账号（有免费额度）

2. **创建对象存储空间**
   - 登录控制台 → 对象存储 → 新建空间
   - 空间名称：`resume-editor`（自定义）
   - 访问控制：选择"公开空间"（用于静态网站）

3. **配置静态网站托管**
   - 进入空间 → 空间设置 → 静态网站托管
   - 开启"静态网站托管"
   - 首页：`index.html`
   - 404 页面：`index.html`（SPA 应用需要）

4. **上传文件**
   ```bash
   npm run build
   # 使用七牛云控制台上传 dist 目录下的所有文件
   # 或者使用 qshell 命令行工具
   ```

5. **绑定 CDN 域名（可选）**
   - 在空间设置中绑定自定义域名
   - 配置 HTTPS 证书
   - 国内访问速度会更快

**访问地址：**
- 默认地址：`http://你的空间名.bkt.clouddn.com`
- 自定义域名：`https://你的域名.com`

## 📊 各方案对比

| 方案 | 费用 | 国内访问速度 | 配置难度 | 推荐度 |
|------|------|------------|---------|--------|
| ~~Gitee Pages~~ | ~~免费~~ | ~~⭐⭐⭐⭐⭐~~ | ~~⭐⭐~~ | ~~已下线~~ |
| 七牛云 | 免费（有额度） | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| 阿里云 OSS+CDN | 付费 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| 腾讯云 COS+CDN | 付费 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| Netlify | 免费 | ⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐ |
| Vercel + CDN | 免费 | ⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐ |

## 🚀 快速开始（Gitee Pages）

### 1. 导入仓库到 Gitee

```bash
# 在 Gitee 网页上操作，或使用命令行：
git remote add gitee https://gitee.com/your-username/resume_editor.git
git push gitee main
```

### 2. 配置 GitHub Actions 自动部署到 Gitee

创建 `.github/workflows/deploy-gitee.yml`：

```yaml
name: Deploy to Gitee Pages

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      
      - name: Install dependencies
        run: npm install
      
      - name: Build
        run: npm run build
      
      - name: Deploy to Gitee
        uses: yanglbme/gitee-pages-action@main
        with:
          gitee-username: your-username
          gitee-password: ${{ secrets.GITEE_PASSWORD }}
          gitee-repo: resume_editor
          branch: main
          directory: dist
```

### 3. 配置 Gitee Token

在 GitHub Secrets 中添加 `GITEE_PASSWORD`（你的 Gitee 密码或 Token）

## 💡 建议

**最推荐使用七牛云**：
- ✅ 免费额度充足（10GB 存储 + 10GB 流量/月）
- ✅ 国内访问速度快
- ✅ 配置相对简单
- ✅ 支持自定义域名和 HTTPS

**备选方案：Netlify**
- ✅ 完全免费
- ✅ 配置简单
- ✅ 自动部署
- ⚠️ 国内访问速度一般

**如果预算充足：**
- 阿里云 OSS + CDN（最稳定，国内访问最快）
- 腾讯云 COS + CDN（类似阿里云）

