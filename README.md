<div align="center">
<img width="1200" height="475" alt="简历佳 Banner" src="https://github.com/user-attachments/assets/0aa67016-6eaf-458a-adb2-6e31a0763ed6" />
</div>

# 📝 简历佳 - 简历编辑器

一个专业的、可编辑的简历构建工具。

## ✨ 功能特性

- 🎨 **多种精美模板**：提供经典简约、财务职场、侧栏开发三种专业简历模板
- 💾 **本地存储**：所有数据保存在本地，保护隐私安全
- 📥 **导出 PDF**：一键导出高质量 PDF 简历
- 📱 **响应式设计**：完美适配桌面和移动设备
- 🎯 **实时预览**：编辑时实时预览简历效果

## 🚀 快速开始

### 环境要求

- Node.js 16+ 
- npm 或 yarn

### 安装步骤

1. **克隆项目**
   ```bash
   git clone https://github.com/your-username/ai-resume-builder.git
   cd ai-resume-builder
   ```

2. **安装依赖**
   ```bash
   npm install
   ```

3. **启动开发服务器**
   ```bash
   npm run dev
   ```

5. **打开浏览器**
   
   访问 `http://localhost:3000` 开始使用

## 📦 构建生产版本

```bash
npm run build
```

构建完成后，静态文件将生成在 `dist` 目录中。

预览生产构建：
```bash
npm run preview
```

## 🌐 部署

### 🇨🇳 国内访问（推荐）

如果希望国内用户也能快速访问，推荐使用 **七牛云**：

1. 注册七牛云账号（https://www.qiniu.com）
2. 创建对象存储空间并配置静态网站托管
3. 上传构建后的 `dist` 目录
4. 详细步骤请查看 [DEPLOY_CN.md](./DEPLOY_CN.md)

> ⚠️ **注意**：Gitee Pages 已于 2024 年 5 月下线，不再提供服务。

### GitHub Pages

1. 在 `vite.config.ts` 中添加 `base` 配置：
   ```typescript
   export default defineConfig({
     base: '/your-repo-name/', // 替换为你的仓库名
     // ... 其他配置
   })
   ```

2. 安装 GitHub Pages 部署插件：
   ```bash
   npm install --save-dev gh-pages
   ```

3. 在 `package.json` 中添加部署脚本：
   ```json
   {
     "scripts": {
       "deploy": "npm run build && gh-pages -d dist"
     }
   }
   ```

4. 运行部署：
   ```bash
   npm run deploy
   ```

### Vercel

1. 安装 Vercel CLI：
   ```bash
   npm i -g vercel
   ```

2. 在项目根目录运行：
   ```bash
   vercel
   ```

3. 按照提示完成部署

### Netlify

1. 将项目推送到 GitHub
2. 在 [Netlify](https://www.netlify.com/) 中导入项目
3. 构建命令：`npm run build`
4. 发布目录：`dist`

## 🛠️ 技术栈

- **React 19** - UI 框架
- **TypeScript** - 类型安全
- **Vite** - 构建工具
- **Tailwind CSS** - 样式框架
- **Lucide React** - 图标库
- **html2pdf.js** - PDF 导出

## 📖 使用指南

### 基础编辑

1. **填写个人信息**：在左侧编辑面板填写姓名、求职意向、联系方式等
2. **添加教育经历**：点击"添加教育背景"按钮，填写学校、专业、时间等信息
3. **添加工作经历**：点击"添加工作经历"按钮，填写公司、职位、时间、工作内容等
4. **切换模板**：在编辑面板顶部选择不同的简历模板

### 导出简历

1. 点击右上角"导出 PDF"按钮
2. 等待 PDF 生成完成
3. 下载保存到本地

## 📁 项目结构

```
ai-resume-builder/
├── components/          # React 组件
│   ├── ResumeEditor.tsx    # 简历编辑器
│   └── ResumePreview.tsx   # 简历预览
├── types.ts            # TypeScript 类型定义
├── constants.tsx       # 常量定义
├── App.tsx             # 主应用组件
├── index.tsx           # 入口文件
├── vite.config.ts      # Vite 配置
└── package.json        # 项目配置
```

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📄 许可证

本项目采用 [MIT License](LICENSE) 许可证。

## 🙏 致谢

- [Vite](https://vitejs.dev/) - 优秀的构建工具
- [Tailwind CSS](https://tailwindcss.com/) - 实用优先的 CSS 框架

## 📮 联系方式

如有问题或建议，欢迎通过以下方式联系：

- 提交 [Issue](https://github.com/your-username/ai-resume-builder/issues)
- 发送邮件至：your-email@example.com

---

⭐ 如果这个项目对你有帮助，请给个 Star 支持一下！
